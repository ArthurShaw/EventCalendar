<h1>Editing Meeting</h1>

<%= render 'form' %>

<div><%= link_to 'Delete', @meeting, method: :delete, data: { confirm: 'Are you sure?' } %></div>
<%= link_to 'Show', @meeting %> |
<%= link_to 'Back', meetings_path %>
